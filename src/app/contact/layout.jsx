export const metadata = {
  title: "StudyNook – Contact",
  description: "Get in touch with the StudyNook support team.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
