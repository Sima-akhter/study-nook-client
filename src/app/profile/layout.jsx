export const metadata = {
  title: "StudyNook – Profile",
  description: "View and manage your StudyNook profile, bookings, and listings.",
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}
